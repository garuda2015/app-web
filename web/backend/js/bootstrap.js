'use strict';
define(['angular', 'app', 'uiRouter', 'route', 's/InterceptorService', 's/AuthService'], 
		function(angular, app){
			angular.element(document).ready(function(){
				angular.bootstrap(document, ['app']);
			});
	});